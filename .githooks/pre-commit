#!/bin/sh

echo "🔍 Running pre-commit checks..."
echo

echo "▶ Running make lint-strict..."
if ! make lint-strict; then
    echo
    echo "❌ Commit blocked!"
    echo "Fix flake8/mypy errors before committing."
    exit 1
fi

echo
echo "✅ All checks passed."
echo "Commit allowed."

exit 0
